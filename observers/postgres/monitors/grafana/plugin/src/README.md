# Witness data source

Browse `witness.*` Postgres tables from Grafana: full-text/substring event
search, distributed trace reconstruction, a logs panel, and a spans table.

Requires the v1 + v2 migrations and the view set applied to your witness
database (see `../README.md`).
